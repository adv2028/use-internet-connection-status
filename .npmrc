ignoreScripts = true # Prevent scripts from running during installation
engine-strict = true # Fail if the current Node version doesn't satisfy the engines field in package.json
audit = true # Report vulnerabilities in dependencies
minimumReleaseAge = 10080 # Prevent installing packages released in the last 7 days (10080 minutes)
